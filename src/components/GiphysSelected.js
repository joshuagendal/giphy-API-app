import React from 'react';

const GiphysSelected = (props) => {
	const selectedGiphys = props.selectedGiphys;
	console.log(selectedGiphys);
	
	return (
		<h1>GIPHYS SELECTED COMPONENT</h1>
	);
};

export default GiphysSelected;